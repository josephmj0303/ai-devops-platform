# Kubernetes Operations

The platform integrates with Kubernetes through the backend service.

## Inspection

The platform can inspect:

- Cluster nodes
- Pods
- Deployments

## Controlled Operations

Selected operational actions are exposed through the DevOps action
layer. Deployment restart is currently supported.

Kubernetes operations are persisted in the DevOps action history.
